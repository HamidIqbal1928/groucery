import { createStore } from 'vuex';

const store = createStore({
  state() {
    return {
      // Your state
    };
  },
  mutations: {
    // Your mutations
  },
  actions: {
    // Your actions
  },
  getters: {
    // Your getters
  }
});

export default store;
